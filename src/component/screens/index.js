import React from 'react'
import Screen from './Screen'
import avatar from '../image/avatar.png'

export const Profile = ({ navigation }) => <Screen navigation={navigation} name="Profile" />
export const Message = ({ navigation }) => <Screen navigation={navigation} name="Profile" />
export const Activity = ({ navigation }) => <Screen navigation={navigation} name="Profile" />